# Dependencies
node_modules/
frontend/node_modules/

# Production builds
frontend/build/
dist/

# Configuration files (runtime / may contain sensitive data)
config/server-config.json
config/mods.json
config/internal-api-key.json
config/users.json
config/discord-config.json
config/scheduled-tasks.json
config/battlelog.json
config/sso-clients.json
config/sessions.json

# Local assistant/IDE state
.claude/settings.local.json

# Logs
*.log
logs/

# Environment variables
.env
.env.local

# OS files
.DS_Store
Thumbs.db
desktop.ini

# IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# Server files (these are downloaded during installation)
# Don't commit the actual server installation
# ArmaReforgerServer/

# Temporary files
tmp/
temp/
*.tmp
tmp-*.html

# Backups (runtime artifacts)
backups/

# Flute CMS runtime files (if Flute is cloned locally, not as submodule)
flute/storage/
flute/bootstrap/cache/
flute/vendor/
flute/.env
flute/public/uploads/
flute/public/storage

# Flute database credentials
config/flute-db.json

# Test coverage
coverage/
*.lcov

# NPM (lockfiles SHOULD be committed for reproducible + safer installs)
